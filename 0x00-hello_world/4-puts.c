# include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	const char *puzzle = "\"Programming is like building a multilingual puzzle";
	puts(puzzle);
	return (0);
}
